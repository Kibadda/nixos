image:
  tag: v1.106.1
immich:
  persistence:
    library:
      existingClaim: immich-library
server:
  ingress:
    main:
      enabled: true
      hosts:
        - host: "immich.home"
          paths:
            - path: "/"
postgresql:
  enabled: true
resis:
  enabled: true
machine-learning:
  enabled: false
